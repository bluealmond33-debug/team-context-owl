# CODEOWNERS — owners who approve structure/context changes
# Works together with branch protection's "Require review from Code Owners".
# (This file alone only auto-requests review; enforcement is switched on in
#  Settings/rulesets → see _system/GOVERNANCE.md, including the free-plan caveat.)
#
# Replace {{OWNER_HANDLE}} with the integrator's or lead's GitHub handle —
# the kickoff interview offers to do this for you.
# Multiple owners: `* @alice @bob`. To narrow to the lead alone later, leave one handle.
#
# ⚠️ A wrong handle silently disables that owner's required approval — GitHub
#    ignores unknown owners without failing. Double-check the spelling.

* @{{OWNER_HANDLE}}
